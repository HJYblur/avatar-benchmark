## Minimal requirements for avatar-benchmark (excluding `nlf/` subpackage)
numpy>=1.21.0
opencv-python>=4.5.0

## Important: install PyTorch first using the official selector
## (https://pytorch.org) to match the CUDA / OS configuration, then
## run: python -m pip install -r requirements.txt
lightning>=2.0.0

pillow>=8.0.0
omegaconf>=2.1.0
h5py>=3.1.0
tqdm>=4.62.0

# 3D / geometry libs used by core code (avatar templates / ply IO)
trimesh>=3.9.0
plyfile>=0.7.2
pyrender>=0.1.45

# Notes:
# - The `nlf/` subpackage (SMPL/SMPLX fitting, rendering, and demo code)
#   depends on additional packages (smplx, chumpy, pyrender, florch,
#   simplepyutils, matplotlib, imageio, pandas, etc.) which are intentionally
#   omitted here per your request. 
#   If you later need the nlf demos, install
#   them using `nlf/environment_comfortable_py10.yml` or
#   `nlf/install_dependencies.sh`.